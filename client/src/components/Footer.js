import styled from "styled-components";

const Footer = () => {
  return <SiteFooter>This is Footer</SiteFooter>;
};

const SiteFooter = styled.footer`
  background-color: yellow;
  border: 2px solid red;
`;

export default Footer;
